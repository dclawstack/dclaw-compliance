apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-compliance.fullname" . }}
  labels:
    {{- include "dclaw-compliance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-compliance.selectorLabels" . | nindent 4 }}
